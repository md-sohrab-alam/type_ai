# Project-wide Gradle settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
org.gradle.caching=true

# OpenAI API Key (set your API key here or use environment variable)
# For security, do not commit your actual API key to the repository
# Instead, set it as an environment variable or use a local.properties file
OPENAI_API_KEY=